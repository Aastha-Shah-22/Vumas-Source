# # .github/workflows/security.yml
# # This is the "Phase 1" pipeline and lives in your Vumas-Source repository

# name: Phase 1 - Core Security Scans

# on:
#   push:
#     branches: [ main ] # runs on pushes to main branch
#   pull_request: # runs on all pull requests
#   workflow_dispatch: # allows manual triggering of the workflow from github UI

# jobs:
#   core-security-scans: # job name
#     runs-on: ubuntu-latest #using ubuntu as runner environment

#     steps:
#       # Step 1: Checkout the code from *this* repository (clone)
#       - name: Checkout Repository
#         uses: actions/checkout@v4

#       # Step 2: Secrets Detection (Gitleaks)
#       # This will scan the repository's full history for secrets
#       - name: Run Gitleaks Scan
#         uses: gitleaks/gitleaks-action@v2
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#           # This fails the build if leaks are found
#           GITLEAKS_ENABLE_FAIL: true

#       # Step 3: Static Analysis (SAST with Semgrep)
#       # We will output the results to a SARIF file
#       - name: Run Semgrep SAST
#         uses: returntocorp/semgrep-action@v1
#         with:
#           # Use a broad, community-driven ruleset
#           config: p/security-audit
#           # Generate a SARIF file for uploading
#           generateSarif: true
#           output: 'semgrep.sarif'

#       # Step 4: Upload Semgrep SARIF to Security Tab
#       - name: Upload Semgrep Results
#         uses: github/codeql-action/upload-sarif@v3
#         with:
#           sarif_file: semgrep.sarif
#           category: semgrep

#       # Step 5: Dependency & Filesystem Scan (Trivy)
#       - name: Run Trivy Filesystem Scan
#         uses: aquasecurity/trivy-action@master
#         with:
#           # Scan the entire filesystem for vulnerabilities (inc. requirements.txt)
#           scan-type: 'fs'
#           # Only check for vulnerabilities
#           security-checks: 'vuln'
#           # Ignore vulnerabilities that are not fixed yet
#           ignore-unfixed: true
#           # Generate a SARIF file
#           format: 'sarif'
#           output: 'trivy-fs-results.sarif'
#           # Fail the build if high or critical vulnerabilities are found
#           severity: 'HIGH,CRITICAL'

#       # Step 6: Upload Trivy Results to Security Tab
#       - name: Upload Trivy FS Results
#         uses: github/codeql-action/upload-sarif@v3
#         with:
#           sarif_file: 'trivy-fs-results.sarif'
#           category: trivy